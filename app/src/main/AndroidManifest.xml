<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.umd.cs.queuelist">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StudentNameActivity" android:label="Student Check-In"></activity>
        <activity android:name=".StudentQueueActivity" android:label="Student Queue"></activity>
        <activity android:name=".InstructorLoginActivity" android:label="Instructor Login"></activity>
        <activity android:name=".InstructorViewActivity" android:label="Instructor View"></activity>
        <activity android:name=".InstructorQueueActivity" android:label="Student Queue"></activity>
        <activity android:name=".InstructorProjectAddActivity" android:label="Add Project"></activity>
    </application>

</manifest>